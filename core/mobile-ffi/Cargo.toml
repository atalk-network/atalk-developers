[package]
name = "atalk-core-mobile"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[lib]
crate-type = ["staticlib", "cdylib"]

[dependencies]
atalk-core = { path = "../rust" }
serde_json = "1.0.151"

[target.'cfg(target_os = "android")'.dependencies]
jni = "0.21.1"
